# Core dependencies
PyJWT>=2.8.0
cryptography>=41.0.0
pydantic>=2.0.0

# Optional dependencies for specific connection types
websockets>=12.0  # For WebSocket connections
aiohttp>=3.9      # For HTTP/HTTPS connections